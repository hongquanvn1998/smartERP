<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="managebook.list">
      <field name="name">managebook list</field>
      <field name="model">managebook.managebook</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="managebook.action_window">
      <field name="name">managebook window</field>
      <field name="res_model">managebook.managebook</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="managebook.action_server">
      <field name="name">managebook server</field>
      <field name="model_id" ref="model_managebook_managebook"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="managebook" id="managebook.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="managebook.menu_1" parent="managebook.menu_root"/>
    <menuitem name="Menu 2" id="managebook.menu_2" parent="managebook.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="managebook.menu_1_list" parent="managebook.menu_1"
              action="managebook.action_window"/>
    <menuitem name="Server to list" id="managebook" parent="managebook.menu_2"
              action="managebook.action_server"/>
    -->
  </data>
</odoo>