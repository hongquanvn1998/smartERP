<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="savelink.list">
      <field name="name">savelink list</field>
      <field name="model">savelink.savelink</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="savelink.action_window">
      <field name="name">savelink window</field>
      <field name="res_model">savelink.savelink</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="savelink.action_server">
      <field name="name">savelink server</field>
      <field name="model_id" ref="model_savelink_savelink"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="savelink" id="savelink.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="savelink.menu_1" parent="savelink.menu_root"/>
    <menuitem name="Menu 2" id="savelink.menu_2" parent="savelink.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="savelink.menu_1_list" parent="savelink.menu_1"
              action="savelink.action_window"/>
    <menuitem name="Server to list" id="savelink" parent="savelink.menu_2"
              action="savelink.action_server"/>
    -->
  </data>
</odoo>